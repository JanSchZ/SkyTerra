# SkyTerra Backend (.env example)
# Ubicación sugerida: services/api/.env
# Copia este archivo y reemplaza los valores placeholders. Si prefieres mantener
# un único .env en la raíz del monorepo, puedes duplicar estas claves allí; Django
# carga automáticamente el archivo apropiado cuando ejecutas manage.py.

# -----------------------------------------------------------------------------
# Core Django
SECRET_KEY=replace_with_a_secure_key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CLIENT_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database
# - Para desarrollo con SQLite, deja DATABASE_URL vacío o comenta la línea.
# - Para Postgres local usa, por ejemplo:
#   DATABASE_URL=postgres://skyterra:skyterra@localhost:5432/skyterra
# - En Railway u otro proveedor: copia la URL proporcionada por el servicio.
DATABASE_URL=
DB_SSL_REQUIRE=False

# Redis opcional (caché/sesiones). Usa `redis://localhost:6379/0` en local con Docker
# o comenta para desactivar.
REDIS_URL=

# -----------------------------------------------------------------------------
# Stripe
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Coinbase Commerce (pagos cripto, opcional)
COINBASE_COMMERCE_API_KEY=
COINBASE_COMMERCE_WEBHOOK_SECRET=
COINBASE_COMMERCE_DEFAULT_CURRENCY=USD

# -----------------------------------------------------------------------------
# Google Gemini (IA)
GOOGLE_GEMINI_API_KEY=

# OAuth social (habilita solo los proveedores que uses)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=
APPLE_TEAM_ID=
APPLE_KEY_ID=

# -----------------------------------------------------------------------------
# Storage S3/R2 (deja USE_S3=False para almacenar en disco local)
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1
AWS_S3_ENDPOINT_URL=
AWS_S3_CUSTOM_DOMAIN=
AWS_S3_SIGNATURE_VERSION=s3v4
AWS_S3_ADDRESSING_STYLE=auto
MEDIA_UPLOADS_PREFIX=uploads/

# -----------------------------------------------------------------------------
# Email (SMTP)
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=SkyTerra <noreply@example.com>
SERVER_EMAIL=SkyTerra Server <ops@example.com>
ADMIN_EMAIL=admin@example.com
ADMIN_NOTIFICATION_EMAILS=ops@example.com,cto@example.com

# -----------------------------------------------------------------------------
# Logging y seguridad
LOG_LEVEL=INFO
SECURE_REFERRER_POLICY=strict-origin-when-cross-origin
SECURE_HSTS_SECONDS=31536000

# -----------------------------------------------------------------------------
# Buenas prácticas
# 1. Nunca subas tu .env real al repositorio.
# 2. Usa valores distintos para desarrollo, staging y producción.
# 3. Coordina con DevOps para rotar claves sensibles periódicamente.
