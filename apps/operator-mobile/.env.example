# SkyTerra Operator App (.env example)
# Copia este archivo a apps/operator-mobile/.env antes de ejecutar Expo.

# URL base del backend. Para desarrollo levanta Django en localhost:8000.
API_URL=http://localhost:8000

# Si usas Strapi/feature flags u otros servicios móviles, documenta aquí sus claves.
# API_TOKEN=
# SENTRY_DSN=

# Notas:
# 1. Expo lee este archivo gracias a `dotenv/config` en app.config.ts.
# 2. Reinicia `npm run start` tras modificar este archivo para propagar los cambios.
# 3. No compartas tus credenciales en commits o capturas de pantalla.
